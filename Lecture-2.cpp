#include <iostream>     //this is a header
using namespace std;        //std makes sure that cout is a part of "iostream"

int main() {
    cout<<"Hello World"<<endl;      //Output stream, which is displayed on the terminal
    int num;
    cin>>num;       //Input Stream, which takes input from the user
    
    return 0;
}